# EXP-20260902-002-virtual-wrist-segment-contact-scan: Experiment Plan

问题：`EXP-20260901-002` 只记录离散候选关节配置的静态接触。对默认配置和该 run 的
`first_maximum_zero_contact` 端点之间，65 个等间隔关节配置在 10 个已记录杯位下的 MuJoCo
接触数与腕部虚拟相机几何可见数如何变化？

输入和控制项：端点和静态接触结果取自 `EXP-20260901-002`，杯位和默认配置取自
`EXP-20260901-001`；两者的模型 revision 和杯位数量必须一致。对端点之间的 65 个等间隔
关节配置逐一作前向运动学计算，并对每个杯位记录 `qpos`、`ncon` 和几何可见数。禁止
actuation、控制器、动力学步进、IK、轨迹优化和硬件 I/O。

判据：完整记录 65 个配置、源端点索引、关节限位检查、每配置的全部接触数和可见杯数；只以产物
完整性作为 run 通过条件。任何出现的接触都保留，不以其是否为零决定 run 成败。

局限：线性关节插值只是离散配置扫描，不是时间参数化轨迹；未检查速度、加速度、力矩、连续碰撞、
真实碰撞模型、真实相机或真机校准。零接触离散样本不能推出中间连续无碰撞或任何真机能力。

预期产物：不可覆盖的 formal run manifest、完整配置段表、结果与配对 experiment record。结果
保持 `pending_human_review`。
