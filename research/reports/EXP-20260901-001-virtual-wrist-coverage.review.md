# EXP-20260901-001-virtual-wrist-coverage: Technical Review Note

技术校验：formal manifest 与 result/record 配对均通过；人工审查状态为 `pending_human_review`。

- 默认候选模型姿态的腕部相机是 0/10 几何可见；
- 257 个记录姿态中，第 229 个是第一个达到最大值的样本，为 10/10；
- 该样本的 RGB 画面确实包含 cup，但相机同时贴近或穿过候选模型几何。

本 run 没有执行碰撞检查、姿态可达性、关节安全、运动规划或真机校准。因此“存在 10/10 静态视线样本”
只是一条候选模型内的可见性事实，不能成为可执行姿态、腕相机布局或真机能力的采纳依据。
