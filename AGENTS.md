# First-Robots 协作规则

- 修改前说明假设、范围、成功标准和验收证据；失败、跳过和不确定性必须显式报告。
- 先读 `project.json` 及其中列出的 overview、architecture、status，再决定是否需要读代码或研究旁车。
- `src/`、`config/`、`scripts/`、`tests/` 不得导入或读取 `research/`；产品运行不依赖研究存在。
- 脚本只做参数解析和调用公开源码模块；脚本之间不得互相导入。
- 共享记忆必须是带来源、时间、置信度和版本的结构化事实；自然语言摘要不是唯一事实源。
- LLM/Agent 不直接计算或绕过关节限位、负载、碰撞、可达性和安全门禁；这些由确定性模块验证。
- 机器人能力只能由模型事实或可复现实验支持；单次成功/失败不得自动扩大或缩小安全边界。
- 重大架构、接口、迁移、部署或风险变更使用 `changes/CHG-...json`；需要研究依据时关联正式 run。
- 正式研究数据、配置、种子、运行和产物只放 `research/`；已校验证据不等于人工采纳。
- 阈值、因果结论、高风险发布、能力版本采纳和研究删除必须由人确认。
